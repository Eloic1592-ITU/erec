{# ---------------------------- Layout ---------------------------- #}
{% extends 'back/layouts/base.html.twig' %}

{# ---------------------------- Title ---------------------------- #}
{% block title %}
    Dashboard | Expériences professionnelles
{% endblock %}

{# ---------------------------- CSS ---------------------------- #}
{% block stylesheets %}
  <link rel="stylesheet" href="{{ asset('css/back/user.css') }}" />
{% endblock %}

{# ---------------------------- Body ---------------------------- #}
{% block body %}
    
    {{ include('back/partials/_header.html.twig') }}

    <div class="container">
        <div class="row">
            <div class="col-lg-12">
                <div class="card">
                    <div class="card-header">
                        <h3 class="card-title">Liste des postes déjà occupés par <span class="text-primary">{{ candidate.getFirstName() }}</span></h3>
                    </div>
                    <div class="card-body p-0">
                        {{ include('back/partials/tables/candidate/_work_experiences.html.twig') }}
                    </div>
                </div>
            </div>
        </div>
    </div>
    
{% endblock %}

{# ---------------------------- JS ---------------------------- #}
{% block javascripts %}
  {# ... #}
{% endblock %}

