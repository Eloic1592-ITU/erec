<!-- Add Campaign -->

<div
  id="campaign-modal"
  class="modal modal-sm fade"
  data-bs-backdrop="static"
  data-bs-keyboard="false"
  tabindex="-1"
  aria-hidden="true"
>
  {{ form_start(newCampaignForm, {
    'attr': {
      'id': 'campaign-new',
      'class': 'needs-validation',
      'novalidate': '',
    }
  }) }}
    <div class="modal-dialog modal-dialog-centered">
      <div class="modal-content">
        <div class="modal-header px-4">
            Titre de Campagne
          <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body text-start p-3">
          {{ form_widget(newCampaignForm.title) }}
        </div>
        <div class="modal-body text-start p-3">
          {{ form_widget(newCampaignForm.description) }}
        </div>
        <div class="modal-footer d-flex flex-nowrap gap-1 border-0 pb-4 px-4 pt-0">
          <button id="campaign-new-submit" type="submit" type="button" class="btn btn-primary w-100 d-flex justify-content-center align-items-center gap-2 m-0">
            <i class="bx bx-check"></i> <span class="">Enregistrer</span>
          </button>

          <button id="campaign-new-loader" class="btn btn-primary m-0 w-100 d-none" type="button" disabled>
            <span class="spinner-border spinner-border-sm" aria-hidden="true"></span>
            <span role="status">Enregistrement...</span>
          </button>
        </div>
      </div>
    </div>
  {{ form_end(newCampaignForm) }}   
</div>