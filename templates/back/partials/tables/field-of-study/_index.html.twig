<table id="data-list" class="table table-striped nowrap" style="width: 100%">
    <thead>
    <tr>
        <th>ID</th>
        <th>Nom</th>
        <th>Statut</th>
        <th>Actions</th>
        <th>Détails</th>
    </tr>
    </thead>
    <tbody>
    {% for fieldOfStudy in allfieldOfStudy %}
        <tr>
            <td>{{ fieldOfStudy.getId() }}</td>
            <td>{{ fieldOfStudy.getName() }}</td>
            <td>
                {% if not fieldOfStudy.isDeleted() %}
                    <span class="badge text-bg-success rounded-pill">Activé</span> 
                {% else %}
                    <span class="badge text-bg-danger rounded-pill">Desactivé</span>
                {% endif %}
            </td>
            <td>
                <div class="d-flex justify-content-end gap-1">
                    <button 
                        class="btn-action bg-warning" 
                        data-bs-toggle="modal" 
                        data-bs-target="#field-of-study-{{ fieldOfStudy.getId() }}"
                    >
                        <i class="bx bx-edit"></i> 
                        <span>Modifier</span>
                    </button>

                    {% if not fieldOfStudy.isDeleted() %}
                        <button 
                            class="btn-action btn-state bg-danger delete-field-of-study"
                            data-state="désactiver ce domaine d'études" 
                            data-url="{{ path('admin_field_of_study_disable', {'id': fieldOfStudy.getId()}) }}"
                        >
                            <span>Désactiver</span>
                        </button>
                    {% else %}
                        <button 
                            class="btn-action btn-state bg-success delete-field-of-study"
                            data-state="réactiver ce domaine d'études" 
                            data-url="{{ path('admin_field_of_study_activate', {'id': fieldOfStudy.getId()}) }}"
                        >
                            <span>Activer</span>
                        </button>
                    {% endif %}

                </div>
            </td>
            <td><a class='btn btn-warning btn-sm bg-warning text-white w-100' href='#'><i class='bx bx-info-circle me-2'></i>Voir plus</a></td>
        </tr>
    {% endfor %}
    </tbody>
</table>

{# Modal for Adding new item #}
{{ include('back/partials/modals/_newFieldOfStudy.html.twig') }}

{# Modals for Editing each items #}
{% for fieldOfStudyForm in fieldOfStudyForms %}
    {{ include('back/partials/modals/_editFieldOfStudy.html.twig') }}
{% endfor %}